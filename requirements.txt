# Runtime pins for CI. The authoritative dependency list is [project.dependencies]
# in pyproject.toml; this file exists so workflows install a known-good set.
requests>=2.32
feedparser>=6.0
python-dateutil>=2.9
PyYAML>=6.0
beautifulsoup4>=4.12
